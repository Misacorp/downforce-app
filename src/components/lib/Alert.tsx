import { Alert } from "@react95/core";
import styled from "styled-components/macro";

/**
 * An alert component except it can be used inline.
 */
export default styled(Alert)`
  position: relative;
  transform: none !important;
  top: unset;
`;
